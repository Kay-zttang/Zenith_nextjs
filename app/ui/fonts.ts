import {Inter, Lusitana, Kumar_One} from 'next/font/google';

export const inter = Inter({subsets: ['latin']});

export const lusitana = Lusitana({
    subsets: ['latin'],
    weight: ['400' , '700'],
    
});

export const kumar_one = Kumar_One({
    subsets: ['latin'],
    weight: ['400'],
    
});